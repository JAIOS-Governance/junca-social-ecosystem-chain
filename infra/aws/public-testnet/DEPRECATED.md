# Deprecated duplicate module

This directory is not an apply target.

The single canonical Public Testnet module is:

`infrastructure/aws`

JUNCA Social Ecosystem Chain AWS workflows must reject plans that mix this
legacy module with the canonical module. The directory remains temporarily for
history and migration review only.

Mainnet Changed=false  
Assets Moved=false  
Bridge Activated=false
