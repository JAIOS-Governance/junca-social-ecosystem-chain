global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/junca-rules.yaml

scrape_configs:
  - job_name: junca-otel-collector
    static_configs:
      - targets: ["otel-collector:9464"]
